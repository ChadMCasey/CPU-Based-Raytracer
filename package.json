{
  "devDependencies": {
    "live-server": "^1.2.2",
    "prettier": "^3.9.5",
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  },
  "scripts": {
    "dev": "npx kill-port 5500 && npm run build && vite",
    "build": "npx prettier . --write && tsc && vite build"
  }
}
