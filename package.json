{
  "devDependencies": {
    "live-server": "^1.2.2",
    "prettier": "^3.9.5",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "compile": "tsc",
    "watch": "tsc --watch",
    "serve": "live-server src --entry-file=index.html"
  }
}
